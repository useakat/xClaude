{
  "mcpServers": {
    "mcp-gsheets": {
      "command": "bash",
      "args": ["-c", "exec bash \"${CLAUDE_PROJECT_DIR}/scripts/mcp_gsheets_launch.sh\""],
      "env": {
        "GOOGLE_SERVICE_ACCOUNT_KEY": "${GOOGLE_SERVICE_ACCOUNT_KEY}",
        "GOOGLE_PROJECT_ID": "${GOOGLE_PROJECT_ID}"
      }
    }
  }
}
