---
title: note 下書き保存の再実行を安全にする（--note-id・画像失敗時の停止） — セッション履歴
date: 2026-08-12
sidebar:
  hidden: true
---

# セッション履歴

> 2026-08-12 のセッション作業ログ。

← [報告書へ戻る](../../reports/20260812_note_draft_retry_safety/)

---

## よーん *(15:13 JST)*

両方の対策を行って

### Claude *(15:14 JST)*

*[ツール: `Read`, `Read`, `Edit`, `Edit`, `Edit`, `Edit`]*

Now the CLI argument and the louder failure output.

*[ツール: `Edit`, `Edit`, `Bash`, `Bash`, `Bash`, `Read`, `Edit`, `Bash`, `Bash`]*

両方の対策を実装し、動作確認まで終えました。

**1. `--note-id` で既存下書きを上書き更新**（`scripts/send_note_draft.py`）

```bash
python3 scripts/send_note_draft.py "<タイトル>" --base-dir ... --note-id 174474109 < index.md
```

- 指定時は `text_notes`（新規作成）を呼ばず、その下書きに `draft_save` とアイキャッチ設定だけを行う
- 戻り値に `"mode": "create" | "update"` を追加し、どちらで動いたか一目で分かるようにした
- **確認**: 実記事の下書き 174474109 に対して実行 → `mode: "update"`・同じ note_id・画像11点再アップロード成功。新しい下書きは増えていません

**2. 画像が1点でも失敗したら明示的に止める**

- スクリプト側: 終了コード2に加えて、標準エラーに次を出力するようにした

  > `ERROR: 画像 N 点のアップロードに失敗しました。この下書きは未完成です。原因を直したうえで、同じ下書きに --note-id <id> を付けて再実行してください（新しい下書きを作らないため）。`

- `projects/w002/spec.md` フロー16に明記
  - **`failed_images` が空でなければフロー17以降へ進まない**（画像が欠けた下書きは未完成）
  - 再実行は必ず `--note-id` を付ける
  - フォールバックの `--no-images` は「まず原因特定を試み、どうしても通らない場合のみ」に格下げ（今回のように原因が特定できるケースで安易に手動運用へ戻らないため）。今回の仕様変更の実例も注記

- **確認**: 存在しない画像を含む本文で実行 → 上記メッセージが出て終了コード2

**確認作業でお願いしたいこと**

失敗パスのテストに、削除予定だった空の下書き **174469281** を使いました（新しい下書きを増やさないため）。現在この下書きには「テスト」という本文が入っています。**note 上で削除してください**。記事の正式な下書きは **174474109** です。

この2件も `/record` で記録しますか？

*[ツール: `Bash`]*

**変更ログ候補（未記録）**

以下の変更が記録されていません。記録するものを選んでください：

1. **note 下書き保存の再実行を安全にする（`--note-id` での上書き更新＋画像失敗時の停止）** — 画像だけ失敗したときに下書きを増やさず同じ下書きへ再実行できるようにし、欠けたまま次工程へ進まないよう明示的に止める
   - 関連コミット: なし（**未コミット**・本セッションで実装）
   - 変更ファイル: `scripts/send_note_draft.py`（`--note-id` 追加・`mode` を戻り値に追加・失敗時の stderr メッセージ）、`projects/w002/spec.md`（フロー16に停止条件と再実行手順を明記）
   - 関連する過去の変更: 「note 本文画像アップロードを API 仕様変更に追従（`filename`・埋め込みURL）」（本日・同報告書の「今後の課題」に挙げた2点をそのまま実装）／「note 記事の下書き保存を画像・サムネ込みで自動化（W002 フローに組み込み）」（2026-08-10）

1件だけなので、選択肢を1つ補足します。この変更は本日の報告書 `20260812_note_body_image_upload_api_fix` の「今後の課題」を潰したものなので、**別の報告書を新規作成する**か、**既存報告書に追記して課題を解決済みにする**かを選べます。

- **A（推奨）: 新規報告書＋変更ログ1件** — 独立した改善として残す。既存報告書の「今後の課題」からは新報告書へリンクする
- **B: 既存報告書に追記のみ** — 変更ログは増やさず、既存エントリの報告書内で「対応済み」に更新する

番号（1）と、A / B のどちらかをお知らせください。記録不要なら「スキップ」とお伝えください。

---

## よーん *(15:24 JST)*

A

### Claude *(15:24 JST)*

*[ツール: `Bash`, `Write`, `Read`, `Edit`, `Bash`]*
