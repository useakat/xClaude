---
title: X投稿一覧からランダム選択して【threads投稿】Gmail下書きを自動作成する仕組みを追加 — セッション履歴
date: 2026-07-12
sidebar:
  hidden: true
---

# セッション履歴

> 2026-07-12 のセッション作業ログ。

← [報告書へ戻る](../../reports/20260712_threads_draft_random_pick/)

---
## よーん *(21:13 JST)*

X投稿一覧シートから、ランダムに投稿を選んで、threads 投稿用メールを作成して下書きとして保存するのを自動化したい。ただし一度 threads に投稿した X投稿はえらばないようにする。実装計画を立てて。まだ実装はしないで。

### Claude *(21:22 JST)*

実装計画のため plan mode に入ります。X投稿一覧の構造・重複管理方法・下書き作成の既存手段を調べます。

---

### Claude *(21:25 JST)*

planでの調査を続けます。残りの不明点（下書き作成スクリプト・既存ローカルcronのパターン・重複管理の手がかり）を1つのExploreで確認します。

*[ツール: `Agent`]*

Exploreを待つ間、重複管理の設計に必要な実データ（X投稿一覧のヘッダ・親ポスト列の使われ方、outputsのthreads記録）を読み取りで確認します。

*[ツール: `mcp__mcp-gsheets__sheets_get_values`]*

X投稿一覧の構造を確認しました。本文は500字超（552〜988字）が普通で分割前提、画像URLは pbs.twimg.com がそのまま使え、親ポストURL列でセルフリプ行を判別できます。既存の「threads転載済み」を示す列はありません。

設計を固めるため、Exploreの調査を待ちつつ、独立して決められる点を先に確認します。

*[ツール: `AskUserQuestion`]*

調査完了。設計に必要な情報が揃いました。回答（①転載マーク列を追加 ②セルフリプも含める ③毎日1件）を反映して計画を確定します。

*[ツール: `Write`, `Read`, `Write`]*

計画をまとめました。承認いただければ実装に入ります。

*[ツール: `ExitPlanMode`]*

計画が承認されました。実装を進めます。まず作業を整理します。

*[ツール: `Bash`, `Bash`]*

Pull完了。参照する既存スクリプトを読みます。

*[ツール: `Read`, `Bash`, `Bash`]*

全パターン把握しました。まずマーカー列ヘッダを追加します。

*[ツール: `mcp__mcp-gsheets__sheets_update_values`]*

グリッドが33列までなので、列追加はスクリプト内で冪等に行う方式にします（`add_cols`＋ヘッダ設定）。スクリプトを作成します。

*[ツール: `Write`]*

Now the wrapper:

*[ツール: `Write`, `Bash`]*

Now dry-run test:

*[ツール: `Bash`]*

Dry-run成功（マーカー列AHを新設、候補189件、書式も正しい）。実際に下書きを1件作って動作確認します（下書き作成＋マーク）。

*[ツール: `Bash`]*

下書き作成＆マーク成功。実在確認します（Gmail下書き＋シートのマーカー）。

*[ツール: `Bash`, `Bash`, `ToolSearch`, `mcp__claude_ai_Gmail__list_drafts`]*

下書き実在を確認。マーカーがシートに書かれたか確認します。

*[ツール: `mcp__mcp-gsheets__sheets_get_values`, `mcp__mcp-gsheets__sheets_get_values`]*

マーカー（AH273 = 2026-07-12）も確認できました。全工程が動作しています。cron を登録します。既存 crontab を確認します。

*[ツール: `Bash`, `Bash`]*

cron の自動インストールはセーフティ判定でブロックされました。plan は承認済みですが、永続化 cron の登録はよーん自身に実行いただく形にします。

## 実装完了（cron 登録を除く）

**動作確認済み：**
- `scripts/make_threads_draft.py`（新規）— X投稿一覧からランダム選択 →【threads投稿】下書き作成 → マーク
- `scripts/run_threads_draft.sh`（新規）— cron ラッパー
- X投稿一覧に **AH列「threads転載日」** を新設（スクリプトが冪等に作成）
- dry-run OK → 本番1回 OK：**Gmail下書き1件作成**＋**行273 に `2026-07-12` マーク**を確認
- 候補抽出 189件（全1221行中）、書式（[投稿文]/[画像URL]、必要時[リプ]）も正常

**残り1つ — cron 登録（よーん実行）**
セーフティ判定で私からの crontab 変更がブロックされました。以下を `!` で実行してください（既存 threads cron と同じ crontab に毎朝8:00で追加）：

```
! ( crontab -l; echo "0 8 * * * /bin/bash /root/xClaude/scripts/run_threads_draft.sh" ) | crontab -
```

**運用フロー**：毎朝8:00に下書き自動生成 → よーんが確認・編集して自分宛に送信 → 既存の Threads 投稿 cron（12/17/20時）が拾って投稿。

---

1点、判断を仰ぎたい点があります。今回テストで選ばれたのは「note 50スキ超え！」という **目的=誘導**の告知投稿（t.co リンク入り）で、後日 Threads に転載するには不向きでした。候補を絞るか確認させてください。

*[ツール: `AskUserQuestion`]*

「目的=有益」のみに絞ります。フィルタを追加します。

*[ツール: `Edit`, `Edit`, `Bash`]*

候補が56件（有益のみ）に絞られ、正しく動作しています。実装は完了です。

## 完了サマリー

| 項目 | 状態 |
|---|---|
| `scripts/make_threads_draft.py`（新規） | ✅ ランダム選択→下書き作成→マーク。候補=**通常ツイート/目的=有益/親ポスト空/未転載/本文あり** |
| `scripts/run_threads_draft.sh`（新規） | ✅ cron ラッパー |
| X投稿一覧 AH列「threads転載日」 | ✅ 新設・マーク動作確認 |
| dry-run / 本番1回 | ✅ Gmail下書き生成＋マーク（行273）確認 |
| 目的=有益フィルタ | ✅ 候補 189→56件 |

**残り（よーん実行）— cron 登録**（私からの crontab 変更はセーフティ判定でブロックされたため）：
```
! ( crontab -l; echo "0 8 * * * /bin/bash /root/xClaude/scripts/run_threads_draft.sh" ) | crontab -
```

**補足2点**
- テストで作成した下書き（「note 50スキ超え」＝目的=誘導）が Gmail に1件残っています。不要なら削除してください（行273 はマーク済みですが、有益フィルタにより今後も選ばれません）。
- 運用：毎朝8:00に下書き生成 → 確認・送信 → 既存 Threads 投稿 cron（12/17/20時）が投稿。

この変更を記録しますか？`/record` で記録できます。

---

## よーん *(22:26 JST)*

cron 登録した。

### Claude *(22:27 JST)*

登録を確認します。

*[ツール: `Bash`]*

cron 登録を確認しました（毎朝 8:00）。これで全工程が完了です。

明朝 8:00 から自動で【threads投稿】下書きが1件ずつ作成されます。ログは `logs/threads_draft.log` で確認できます。

新規スクリプト2件（`make_threads_draft.py` / `run_threads_draft.sh`）をコミットしてよいですか？合わせて `/record` で変更ログにも記録できます。

---

## よーん *(22:27 JST)*

試しに cron の処理を１度実行してみて

### Claude *(22:28 JST)*

*[ツール: `Bash`, `mcp__claude_ai_Gmail__list_drafts`]*

✅ cron 処理を1回実行し、正常に完走しました。

**実行結果**
- 候補 56件から行207 を選択（本文664字・画像あり・セルフリプなし）
- Gmail 下書き作成 → シート AH207 に `2026-07-12` マーク → 完了（exit 0）

**作成された下書き**（Gmail で実在確認済み）
- 件名: `【threads投稿】はい、関係はあります。 共通しているのは`
- 内容: 熱圏と太陽コロナの温度に関する質問回答（664字 → 投稿時に既存 cron が500字で自動分割）＋画像URL

1点気づいた点：今回選ばれたのは X 上の**質問への回答投稿**（「はい、関係はあります」で始まる）で、単体で Threads に載せると文脈が分かりにくいかもしれません。目的=有益でもこういう投稿が混ざるので、下書き確認時に不要なら削除してください（削除しても AH マークで再選択はされません）。

現在 Gmail に下書きが2件あります（テスト分の「note 50スキ超え」＋今回分）。不要な方は削除してください。送信すれば次の Threads 投稿 cron（17:00/20:00）が投稿します。

コミットと `/record` はどうしますか？

---

## よーん *(22:33 JST)*

cron の１回の処理で、４つ下書きを作成するように修正して

### Claude *(22:33 JST)*

4件を重複なく選ぶよう修正します（`--count` 引数を追加、cron ラッパーで `--count 4` を指定）。

*[ツール: `Edit`, `Edit`]*

Now restructure main to loop over N picks:

*[ツール: `Edit`, `Edit`, `Edit`]*

dry-run で4件選択を確認します:

*[ツール: `Bash`]*

✅ 修正完了。dry-run で **4件が重複なく選択**されることを確認しました。

**変更内容**
| ファイル | 変更 |
|---|---|
| `scripts/make_threads_draft.py` | `--count N` 引数を追加（`random.sample` で重複なし選択、件数分ループで下書き作成＋マーク） |
| `scripts/run_threads_draft.sh` | `--count 4` を指定（cron 1回で下書き4件） |

- 候補不足時は残り全件だけ作成（ログに注記）
- 途中で下書き作成に失敗した場合、作成済み分はマーク済みのまま exit 1（重複しない）

明朝 8:00 の cron から毎日4件の下書きが作られます。実際に4件作る本番テストもしますか？（実行すると下書き4件＋マーク4行が追加されます）

---

## よーん *(22:37 JST)*

commit & push & record

### Claude *(22:37 JST)*

実装ファイルをコミットしてから、`/record` の手順で記録します。

*[ツール: `Bash`, `Bash`, `Bash`, `Write`]*

