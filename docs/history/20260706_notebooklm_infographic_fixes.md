---
title: notebooklm_manager.py と図解テンプレートのバグ修正2件 — セッション履歴
date: 2026-07-06
sidebar:
  hidden: true
---

# セッション履歴

> 2026-07-06 のセッション作業ログ。

← [報告書1へ戻る](../../reports/20260706_notebooklm_infographic_failure_bug/) ｜ [報告書2へ戻る](../../reports/20260706_w003_infographic_subtitle_lock/)

---

## よーん *(19:29 JST)*

02 のプロンプトを表示して

### Claude *(19:29 JST)*

*[ツール: `Read`]*

02（ステップ・フロー型）のプロンプト全文をお見せしました（`draft/infographic_02.md`）。実際の生成画像で見ると、サブタイトルが「なぜ48年間も無充電で動き続けられるのか、その『原子力電池』の仕組みと驚異的な寿命を解説する。」となっており、指定した「スマホが1日で切れる電源とは違う、原子力電池という発電方式」からNotebookLM側で若干変わっています（内容自体は正確です）。

---

## よーん *(19:32 JST)*

サブタイトルが勝手に変わらないようにするにはどうしたらいい？

### Claude *(19:32 JST)*

*[ツール: `Bash`]*

考えられる原因は、サブタイトルが「概要を説明する普通の文」の形をしているため、AIが（メインタイトルのような「引用すべき決まり文句」ではなく）「言い換えてよい内容説明」だと解釈してしまうことです。実際、メインタイトルはこれまで全カットで一言一句正確でしたが、崩れるのは決まってサブタイトルでした。

対策として有効なのは:

1. **サブタイトルを鉤括弧で囲み、直後に念押しを入れる**: `サブタイトル（概要）：「スマホが1日で切れる電源とは違う、原子力電池という発電方式」（この文字列を一字一句そのまま使用。要約・言い換え禁止）` のように、指示をテキストのすぐ隣に置く（全体の指示リストの中に1回書くだけでは効きにくい）
2. **サブタイトルを短く・キャッチコピー的にする**: 長い説明文ほど「言い換えていい文」と誤解されやすいので、要約文ではなく体言止めや短いフレーズにする

テンプレート（`projects/w003/infographic_template/`）のサブタイトル指定箇所にこの念押しを恒久的に追記しておきましょうか？

---

## よーん *(19:42 JST)*

1の対策を追記して

### Claude *(19:42 JST)*

*[ツール: `Bash`]*

全6テンプレートに同じ形式で存在します。すべて更新します。

*[ツール: `Bash`]*

6テンプレートすべてに反映しました。今後 `/visual_infographic` がこのテンプレートからプロンプトを作成する際、サブタイトルが自動的に鉤括弧＋念押し付きになります。

引き続き、02のインフォグラフィックを `output/` に採用してよろしいですか？（もしくは02のプロンプトを更新して再生成しますか？）

---

## よーん *(19:43 JST)*

02をあと３枚生成して

### Claude *(19:43 JST)*

02のプロンプトに、先ほど更新した鉤括弧＋念押し形式を適用してから、連番06〜08で3枚追加生成します。

*[ツール: `Read`, `Edit`, `Bash`, `Bash`]*

3枚をバックグラウンドで生成中です。完了したらお知らせします。

3枚とも生成完了しました。確認します。

*[ツール: `Read`, `Read`, `Read`, `Bash`]*

プロンプトファイルは正しく step_flow 型でした。生成結果側の問題のようです。ログを確認します。

*[ツール: `Bash`, `Bash`]*

生成結果が正しくありませんでした。ログにRPCエラー（`CREATE_ARTIFACT failed`）が出ており、これが原因で新規生成に失敗し、代わりに直前の05（チェックリスト型）のキャッシュ画像がダウンロードされてしまったようです（3枚とも05と同一）。再実行します。

*[ツール: `Bash`, `Bash`, `Bash`, `Bash`, `Bash`, `Bash`]*

実際のエラー内容を直接確認します。

*[ツール: `Bash`]*

原因が判明しました。**APIのレート制限／クォータ超過**です（今日すでに13枚生成したため）。`notebooklm_manager.py`のバグで、生成失敗時にも直前の成功画像（05）を誤ってダウンロードしてしまい、同じ画像が3枚できていました。

infographic_06〜08.png は削除し、時間を置いて再生成する必要があります。

*[ツール: `AskUserQuestion`, `Bash`]*

誤った複製画像（06〜08.png）は削除し、プロンプト（.md）はそのまま残してあります（レート制限解除後に再生成できます）。

今回は既存の02（ステップ・フロー型、正しく生成済み）を `output/` に採用して進めます。

*[ツール: `Bash`]*

`output/` に確定しました。

## 最終確認（フロー8）

[最終原稿]

実は、僕らの手が届かない場所で、ボイジャーは48年間、一度も充電せず飛び続けている。

スマホの充電は一日で切れるのに、太陽光すら届かない深宇宙で、ボイジャーは半世紀近く発電し続けている。

その鍵は「原子力電池（RTG）」。

それは、プルトニウム238の崩壊熱を、温度差から電気を生む「ゼーベック効果」で電気に変える装置。

可動部も燃料補給もいらず、光ゼロの環境でも数十年発電し続ける。

充電も補給もない。ただ静かに崩壊する原子の熱だけが、探査機を半世紀近く運び続けている。

[/最終原稿]

*[ツール: `Read`]*

この投稿テキストと画像で、**完成・確定してよろしいですか？**（承認後、Gmail下書き作成に進みます）

---

## よーん *(22:54 JST)*

02の追加3枚の生成、いまできる？

### Claude *(22:54 JST)*

レート制限が解除されているか確認してみます。

*[ツール: `Bash`]*

まだレート制限が解除されていません（同じ「API rate limit or quota exceeded」エラー）。日次クォータの可能性が高く、しばらく時間を置く必要がありそうです。今日中に解除されるかは不明なので、明日以降に再挑戦することをおすすめします。

---

## よーん *(15:49 JST)*

02の追加3枚の生成、いまできる？

### Claude *(15:49 JST)*

レート制限が解除されているか、日付も変わったので確認します。

*[ツール: `Bash`]*

レート制限が解除されました。02の追加3枚（06〜08）を正しいプロンプトで生成します。

*[ツール: `Bash`]*

3枚をバックグラウンドで生成中です。完了したらお知らせします。

*[ツール: `Bash`]*

今回は「タスクID...完了待ち」が正しく表示され、全て異なる画像（MD5値が別）になっています。確認します。

*[ツール: `Read`, `Read`, `Read`]*

3枚とも正しく生成され、サブタイトルも指定通り一言一句正確に反映されています（先ほどの鉤括弧＋念押しの修正が効いています）。

- **06**: 円形アイコンでプロセス表現、右側に結論の吹き出し
- **07**: 温度差（高温/低温）の図解付き、1977年→2025年の年表イラスト入り
- **08**: シンプルな丸型アイコン、矢印つなぎでスタート地点あり

現在 `output/` には02を採用済みです。差し替えますか、それとも02のまま確定しますか？

---

## よーん *(22:27 JST)*

02, 06, 07 の図解画像をダウンロードできるリンクを表示して

### Claude *(22:27 JST)*

ローカルファイルのため、Web上のリンクではなくファイルとして直接お送りします。

*[ツール: `SendUserFile`]*

3枚とも送付しました。ダウンロードしてご確認いただけます。この中から `output/` に採用するものを教えてください。

---

## よーん *(23:29 JST)*

https://drive.google.com/file/d/1dGY2XG8tko4ETzJnit6pPl_RB4oqQt4q/view?usp=drive_link　これが図解画像の最終版。02 を少し修正したもの

### Claude *(23:29 JST)*

Google Driveリンクからファイルをダウンロードします。

*[ツール: `Bash`]*

ダウンロード完了しました。内容を確認します。

*[ツール: `Read`, `Bash`]*

`draft/infographic_09.png` として保存済みです（1280×720、spec.mdの規定解像度と一致）。`output/` の採用画像を02→09に差し替えます。

*[ツール: `Bash`]*

`output/` の3種構成（index.md・png・md）を保つため、`infographic_09.md`（生成プロンプト相当の記録）を作成したいのですが、02から具体的にどこを修正されましたか？（記録用に残しておきたいです）

---

## よーん *(23:31 JST)*

* スーパーニャンコを修正
* 右下の吹き出しの中のテキストを

充電も補給もない。静かに崩壊する原子の熱が、探査機を半世紀近く生かしている。

に修正。

### Claude *(23:31 JST)*

*[ツール: `Write`, `Bash`]*

`output/` を最終3点構成（`index.md` / `infographic_09.png` / `infographic_09.md`）に更新しました。

## 最終確認（フロー8）

[最終原稿]

実は、僕らの手が届かない場所で、ボイジャーは48年間、一度も充電せず飛び続けている。

スマホの充電は一日で切れるのに、太陽光すら届かない深宇宙で、ボイジャーは半世紀近く発電し続けている。

その鍵は「原子力電池（RTG）」。

それは、プルトニウム238の崩壊熱を、温度差から電気を生む「ゼーベック効果」で電気に変える装置。

可動部も燃料補給もいらず、光ゼロの環境でも数十年発電し続ける。

充電も補給もない。ただ静かに崩壊する原子の熱だけが、探査機を半世紀近く運び続けている。

[/最終原稿]

*[ツール: `Read`]*

この投稿テキストと画像で、**完成・確定してよろしいですか？**（承認後、Gmail下書き作成に進みます）

---

## よーん *(23:41 JST)*

投稿文は以下にして

実は、僕らの手が届かない場所で、ボイジャーは48年以上、一度も充電せず飛び続けている。

スマホの充電は一日で切れるのに、太陽光発電に頼れない深宇宙で、ボイジャーは半世紀近く電気を生み続けている。

その鍵は「原子力電池（RTG）」。

それは、プルトニウム238が崩壊するときに出す熱を、温度差から電気を生む「ゼーベック効果」で電力に変える装置。
可動部も燃料補給もいらず、光の乏しい宇宙でも何十年も発電し続ける。

ただ静かに崩壊する原子の熱が、いまもボイジャーの声を地球へ届け続けている。

### Claude *(23:41 JST)*

*[ツール: `Bash`]*

238字、推奨レンジ内です。`output/index.md` を更新します。

*[ツール: `Write`, `Write`]*

## 最終確認（フロー8）

[最終原稿]

実は、僕らの手が届かない場所で、ボイジャーは48年以上、一度も充電せず飛び続けている。

スマホの充電は一日で切れるのに、太陽光発電に頼れない深宇宙で、ボイジャーは半世紀近く電気を生み続けている。

その鍵は「原子力電池（RTG）」。

それは、プルトニウム238が崩壊するときに出す熱を、温度差から電気を生む「ゼーベック効果」で電力に変える装置。
可動部も燃料補給もいらず、光の乏しい宇宙でも何十年も発電し続ける。

ただ静かに崩壊する原子の熱が、いまもボイジャーの声を地球へ届け続けている。

[/最終原稿]

画像は `output/infographic_09.png`（採用済み）のままです。この内容で**完成・確定してよろしいですか？**

---

## よーん *(23:42 JST)*

OK

### Claude *(23:43 JST)*

まずネタ使用済み更新（No.89）を行います。

*[ツール: `Bash`]*

Row90がNo.89です。ステータスを更新します。

*[ツール: `Bash`]*

更新完了。Gmail下書きを作成します。

*[ツール: `Bash`]*

タグ抽出を確認できました。件名を組み立てて下書きを作成します。

*[ツール: `Bash`]*

前回同様IDが空欄表示ですが、実際に作成されたか確認します。

*[ツール: `mcp__claude_ai_Gmail__list_drafts`]*

Gmail下書きは正常に作成されています。続けて、チャット履歴の保存とDriveアップロード（フロー10・11）に進みます。

*[ツール: `Bash`, `Bash`, `Bash`, `Read`]*

Line 951（「次の投稿を作成」の直前の区切り）以降が今回のRTG投稿の作業です。トリミングします。

*[ツール: `Bash`]*

トリミング完了。保存します。

*[ツール: `Bash`]*

Driveへフォルダごとアップロードします。

*[ツール: `Bash`]*

Driveアップロード完了です。すべてのフローが完了しました。

## 完了サマリー

- **投稿テーマ**: ボイジャーのRTG（原子力電池）× 48年以上無充電（onePointNeta[89]）
- **原稿**: `output/index.md`（238字、ファクトチェック済み、ブランド適合49/60全項目8点以上、最終文言はユーザー推敲）
- **採用図解**: `output/infographic_09.png`（ステップ・フロー型、02をよーんが手動修正）
- **Sheets**: No.89を使用済みに更新。ネタ2・5（No.103・104）を宇宙分野で追加済み
- **Gmail下書き**: 作成・添付・`[投稿文]`タグとも検証済み
- **チャット履歴**: `chat_history.md` 保存済み
- **Drive**: `xClaude/projects/w003/20260704_RTG無充電電源` にアップロード完了

---

なお、途中発生した2つの技術的な問題（`create-gmail-draft.sh`のID表示不具合、`notebooklm_manager.py`のレート制限時にキャッシュ画像を誤ダウンロードするバグ）は投稿自体には影響していませんが、後者はスクリプトの実際のバグです。修正しますか？（`/record`での記録も合わせて検討できます）

---

## よーん *(23:52 JST)*

修正して

### Claude *(23:52 JST)*

*[ツール: `Bash`, `Read`, `Bash`, `Bash`, `Edit`, `Edit`, `Bash`]*

修正が正しく動作するか、実際のAPIを消費せずに検証します。

*[ツール: `Bash`]*

修正を確認しました。生成失敗時に `download_infographic` が呼ばれず、明確なエラーメッセージと共に exit code 1 で正しく失敗するようになりました（以前は失敗を無視して直前の画像を誤ダウンロードしていました）。

`cmd_infographic` と `cmd_make_infographic` の両方に同じ修正を適用しています。この変更を記録しますか？`/record` で記録できます。

*[ツール: `Read`, `Bash`, `Bash`, `Bash`]*

未記録の変更を確認しました。

---

**変更ログ候補（未記録）**

1. **`notebooklm_manager.py`：インフォグラフィック生成失敗時に直前の画像を誤ダウンロードするバグを修正** — レート制限等でRPC生成が失敗しても `task_id` が空のまま処理が続行し、直前に成功した画像を無言で再ダウンロードしていた不具合を修正。`status.is_complete` を明示チェックし、失敗時はエラーメッセージ付きで exit code 1 にするよう `cmd_infographic`・`cmd_make_infographic` 両方を修正
   - 関連コミット: 未コミット（本セッションでの変更）
   - 変更ファイル: `scripts/notebooklm_manager.py`
   - 関連する過去の変更: なし

2. **W003 図解テンプレートのサブタイトルを鉤括弧＋念押しで一字一句固定** — インフォグラフィック生成時、メインタイトルは正確に再現される一方でサブタイトルだけがAIによって言い換えられる事象が繰り返し発生していたため、`infographic_template/` 配下6テンプレート全てのサブタイトル指定を「鉤括弧で囲み、直後に『一字一句そのまま使用・要約禁止』の念押しを付す」形式に統一
   - 関連コミット: 未コミット（本セッションでの変更）
   - 変更ファイル: `projects/w003/infographic_template/checklist.md`, `compare_contrast.md`, `pyramid.md`, `radial.md`, `step_flow.md`, `timeline.md`
   - 関連する過去の変更: なし

（記録するものを番号で指定してください。すべて記録不要なら「スキップ」と言ってください。）

---

## よーん *(22:41 JST)*

,12

### Claude *(22:41 JST)*

まず2つの変更をそれぞれ個別にコミットします（`git add -A`は使わず、対象ファイルのみ指定）。

*[ツール: `Bash`, `Bash`, `Bash`, `Bash`]*

2つのコミットが完了しました。次にテンプレートを確認して報告書を作成します。

*[ツール: `Read`, `Bash`, `Write`, `Write`]*
