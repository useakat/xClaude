---
title: 定時投稿に X短文フォールバックを追加＋21:00 短文投稿 cron — セッション履歴
date: 2026-06-28
sidebar:
  hidden: true
---

# セッション履歴

> 2026-06-28 のセッション作業ログ。

← [報告書へ戻る](../../reports/20260628_post_fallback_xshort/)

---

## よーん *(13:26 JST)*

X短文投稿の自動投稿は、6:00, 12:00, 17:00 のcron で実行したい。それぞれ、ワンポイント、質問回答、X長文ストーリーの投稿処理を行うが、投稿対象が見つからない場合、代わりにX短文投稿を投稿するようにしたい。また、21:00 には、X短文投稿を投稿する cron を設定したい。実装計画を立てて。

### Claude *(13:27 JST)*

実装計画を立てるため、まず `post_from_email.sh` の挙動（投稿対象なしのときの終了コード・分岐）と各ラッパースクリプトを確認します。

*[ツール: `Read`]*

`post_from_email.sh` は**投稿対象なしのとき専用の終了コードを返していません**（"投稿対象メールなし"とログして exit 0）。フォールバック判定のため終了コードの導入が要ります。残り2つのラッパーと現状の crontab を確認します。

*[ツール: `Bash`]*

全体像を把握しました。`post_from_email.sh` は「投稿対象なし」専用の終了コードを返していないので、フォールバック判定のため終了コードを導入するのが要です。実装計画を提示します。

## 実装計画：6/12/17 に短文フォールバック ＋ 21:00 に短文投稿

### 現状
```
6:00  ワンポイント(W003)   12:00 質問回答(W006)   17:00 X長文(W001)
7,13,19 z01短文(専用)       8:00 z01下書き作成
```
6/12/17 はすでに希望時刻どおり。各ラッパーは `exec post_from_email.sh …` の薄いラッパー。

### 変更内容

**1. `post_from_email.sh` に終了コードを導入**（フォールバック判定の土台）
- `0` = 投稿成功
- `20` = **投稿対象メールなし**（＝フォールバック発火の合図）
- `1` = 投稿は試みたが失敗（X API エラー等。※フォールバックさせない）
- dry-run は `0`

**2. 3つのラッパーに短文フォールバックを追加**（`exec` をやめて rc 判定）
`run_xonepoint_post.sh` / `run_question_post.sh` / `run_xlong_post.sh` を:
```bash
bash "$DIR/post_from_email.sh" "【ワンポイント解説】" W003 x_post_xonepoint.log
[ $? -eq 20 ] && bash "$DIR/post_from_email.sh" "【X短文投稿】" z01 x_post_short.log
```
の形に（キーワード/howID/ログは各自のもの）。**対象なし(20)のときだけ z01 短文を代替投稿**。投稿失敗(1)では代替しない。

**3. crontab の組み替え**
- 6/12/17 はそのまま（ラッパー側でフォールバック）
- **`0 7,13,19 … run_xshort_post.sh` を削除**（旧・専用 z01 投稿。新方式に置換）
- **`0 21 * * * … run_xshort_post.sh` を追加**（21:00 の z01 専用投稿）
- 8:00 の下書き作成はそのまま

### 留意点（要・認識合わせ）
- **z01 下書きの供給**：下書き作成は 8:00 に1日1本だが、新方式だと z01 投稿は最大4回/日（6/12/17 のフォールバック＋21:00）になりうる。下書きは「よーんがレビュー→送信→INBOX 着信」で供給されるので、**在庫が尽きると z01 投稿は空振り（exit 20）**になります。運用しながら下書き本数を調整する想定でよいか。

確認させてください。

*[ツール: `AskUserQuestion`]*

7/13/19 は削除で確定ですね。これで計画が固まりました。

## 最終実装計画

**1. `scripts/post_from_email.sh`** — 終了コード導入
- `0`=投稿成功 / `20`=投稿対象なし（フォールバック合図）/ `1`=投稿失敗（フォールバックしない）/ dry-run=`0`

**2. ラッパー3本にフォールバック追加**（`exec`→ rc 判定方式）
- `run_xonepoint_post.sh`（6:00, W003）→ 対象なしなら z01
- `run_question_post.sh`（12:00, W006）→ 対象なしなら z01
- `run_xlong_post.sh`（17:00, W001）→ 対象なしなら z01

**3. crontab 組み替え**
- 6/12/17：そのまま（ラッパー側でフォールバック）
- `0 7,13,19 … run_xshort_post.sh` を**削除**
- `0 21 * * * … run_xshort_post.sh` を**追加**
- 8:00 下書き作成：そのまま

**結果の cron 構成**
```
6:00  ワンポイント → なければ z01短文
8:00  z01下書き作成
12:00 質問回答    → なければ z01短文
17:00 X長文      → なければ z01短文
21:00 z01短文（専用）
```

**検証**
- `post_from_email.sh --dry-run "存在しないキーワード" z01 x_post_short.log` で exit 20 を確認（対象なし時）。
- 各ラッパーを dry-run 相当で確認、または `! bash scripts/run_xonepoint_post.sh` 実行（実投稿になるため、まず post_from_email の rc 単体テストで確認）。
- `crontab -l` で 7/13/19 削除・21:00 追加を確認。

この計画で実装を進めてよいですか？（スクリプト編集を含むため確認しています）

---

## よーん *(13:31 JST)*

OK。進めて

### Claude *(13:32 JST)*

実装します。まず `post_from_email.sh` に終了コードを導入します。

*[ツール: `Edit`, `Edit`, `Edit`, `Edit`]*

次にラッパー3本にフォールバックを追加します。

*[ツール: `Write`, `Read`, `Read`, `Read`, `Write`, `Write`, `Write`]*

crontab を組み替えます（7/13/19 削除・21:00 追加）。

*[ツール: `Bash`]*

検証します。構文チェックと、終了コード 20（投稿対象なし）の動作を存在しないキーワードで確認します。

*[ツール: `Bash`]*

実装・検証完了しました。

## 結果

**終了コードテスト**: 投稿対象なし → `exit 20` を確認 ✓（フォールバック発火の合図が正しく機能）。構文チェックも全スクリプト OK。

## 最終 cron 構成
```
6:00  ワンポイント → なければ z01短文
8:00  z01下書き作成
12:00 質問回答    → なければ z01短文
17:00 X長文      → なければ z01短文
21:00 z01短文（専用）
```

## 変更内容
| ファイル | 変更 |
|---|---|
| `scripts/post_from_email.sh` | 終了コード導入（0=成功 / 20=対象なし / 1=投稿失敗） |
| `run_xonepoint_post.sh` / `run_question_post.sh` / `run_xlong_post.sh` | `exec`→rc判定。rc=20 のとき z01短文を代替投稿 |
| crontab | 7/13/19 の z01専用を削除、21:00 を追加 |

実投稿テストは安全判定で止まる可能性があるため未実施です（次回 cron 稼働で確認、または dry-run で疎通確認できます）。

留意点（再掲）: z01 下書きは 8:00 に1日1本作成なので、フォールバック＋21:00 で投稿需要が供給を上回ると z01 投稿は空振り（exit 20）になります。運用しながら下書き本数を調整してください。

この変更を `/record` で記録しますか？

STEP 1・2 を実行します。

*[ツール: `Bash`]*

STEP 3: 未記録の変更候補を提案します。

---

**変更ログ候補（未記録）**

1. **定時投稿に X短文フォールバックを追加＋21:00 に短文投稿 cron** — `post_from_email.sh` に終了コード（0=成功/20=対象なし/1=失敗）を導入し、`run_xonepoint_post.sh`・`run_question_post.sh`・`run_xlong_post.sh`（6/12/17時）を「投稿対象なし(20)なら z01短文を代替投稿」する方式に変更。crontab は z01 専用投稿を 7/13/19 から 21:00 の1本に置き換え。
   - 関連コミット: 未コミット（作業ツリー）
   - 変更ファイル: `scripts/post_from_email.sh`、`scripts/run_xonepoint_post.sh`、`scripts/run_question_post.sh`、`scripts/run_xlong_post.sh`、crontab
   - 関連する過去の変更: **z01 短文投稿の cron 自動化（投稿スクリプト新設…）**（06-27）、**z01 下書き作成フェーズの cron 自動化**（06-27）

補足: 作業ツリーの他の変更（`visual_section-imager` SKILL.md、w002 の画像関連ファイル）は**別件**なので記録・コミットに含めません（cron 関連4ファイルのみ選択コミット）。

記録してよいですか？（番号「1」/ スキップ）

---

## よーん *(13:51 JST)*

1

### Claude *(13:51 JST)*

STEP 4: 報告書を作成します。

*[ツール: `Write`]*
