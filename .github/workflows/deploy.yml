name: Deploy Wiki to GitHub Pages

on:
  push:
    branches: [master]
    paths:
      - 'docs/**'
      - 'starlight/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: starlight/package-lock.json

      - name: Install dependencies
        run: cd starlight && npm ci

      - name: Copy docs to Starlight content directory
        run: cp -r docs/* starlight/src/content/docs/

      - name: Build Starlight
        run: cd starlight && npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: starlight/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
