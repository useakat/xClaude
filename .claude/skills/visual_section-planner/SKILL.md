---
name: visual_section-planner
description: 記事の本文を入力に、H2セクションごとに画像案を3つ考え、各画像（図解／イメージ／写真Web取得）の説明を、セクション分けしたmarkdown形式で出力する。生成プロンプトは書かず画像の説明のみ。
---

# visual_section-planner

記事の本文を渡すと、H2 セクションごとに、その内容を補強する画像案を 3 つずつ考え、各画像の説明を出力する。

対象記事（本文テキスト または 本文ファイルパス）: $ARGUMENTS

## 目的

- 各 H2 セクションに配置する画像案を 3 つずつ用意する
- 図解・イメージ・写真（Web取得）から、その内容に最も合う種類を案ごとに選ぶ
- 執筆後の画像準備工程を、再現性のある形で標準化する

## 手順

1. **入力を取得する。** `$ARGUMENTS` の形式を判定する：
   - ファイルパス（`/` を含み、空白・改行のないパス文字列。例 `…/draft/draft.md`）→ Read で読み込む
   - それ以外 → 本文テキストそのものとして扱う

2. **H2 で本文を分割する。** `## ` で始まる行を見出しとして区切る。
   - 導入部（H1 直下〜最初の H2 の手前）は対象外
   - `## 参考情報` セクションは対象外

3. **各 H2 セクションの本文を読み、画像案を 3 つ考える。** そのセクションの内容を最もよく補強する画像を 3 案。種類は案ごとに下表から 1 つ選ぶ（3 案で種類が重複してよい）。

   | 種類 | 用途 | 説明に書くもの |
   |---|---|---|
   | 図解画像 | 仕組み・因果・構造・時系列の視覚化 | 何を表す図か・主要素を日本語で |
   | イメージ画像 | 情景・感情・雰囲気の喚起 | どんな情景・トーンの画像かを日本語で |
   | 写真画像（Web取得） | 実在の人物・機体・史実の実写が要る場合 | Web から取得すべき被写体・年代・探し方の目安 |

4. **各案に画像種類をラベルし、日本語で画像の説明を書く。** 生成プロンプト（構図指示・英語タグ・パラメータ等）は書かない。実在性が必要な対象（実在人物・実機・史実の現場写真）は「写真画像（Web取得）」にする。

5. **セクション分けした markdown で出力する**（下記「出力形式」）。原稿ファイルへの追記・別ファイル保存はしない。

## 出力形式

```
[画像プランニング結果]

## （H2セクションタイトル1）

1. 図解画像 — （画像の説明）
2. イメージ画像 — （画像の説明）
3. 写真画像（Web取得） — （画像の説明）

## （H2セクションタイトル2）

1. …
2. …
3. …

[/画像プランニング結果]
```

## 禁止事項

- 生成プロンプト（構図指示・英語タグ・パラメータ）を書かない。画像の説明のみにする
- 各セクション 3 案を必ず出す。過不足を出さない
- 実在性が要る対象（実在人物・実機・史実の現場）に「図解画像／イメージ画像」を充てない
- 導入部・参考情報セクションを対象にしない
- brand.md に反する煽り・誇張を画像説明に入れない
